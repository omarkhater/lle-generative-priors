services:
  lle:
    build: .
    container_name: mlflow-lle
    environment:
      PYTHONPATH: /app
    volumes:
      - .:/app
    ports:
      - "5000:5000"
    command: ["mlflow", "ui", "--host", "0.0.0.0", "--port", "5000"]