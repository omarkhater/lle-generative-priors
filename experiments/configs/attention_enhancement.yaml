# Configuration for attention enhancement experiments

# Inherit from base configuration
_base_: "base_stage1.yaml"

# Experiment metadata
experiment_name: "attention_enhancements"
run_name: "increased_heads"
description: "Experiment with increased number of attention heads"

# Modified model parameters
num_cross_attn_heads: 12
num_self_attn_heads: 12

# Modified training parameters
num_epochs: 50  # Shorter training for testing